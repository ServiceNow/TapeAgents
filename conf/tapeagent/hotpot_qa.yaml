hydra:
  job:
    chdir: True
  run:
    dir: outputs/${exp_name}_optimize=${optimize.do}_seed=${seed}
target: evaluate
exp_name: ${agent}
seed: 2
agent: ???
llm_cache: true
load_demos: false
optimize:
  do: false
  n_demos: 2
rag:
  partial_demos: true
  demos: true
agentic_rag:
  max_hops: 2
dataset:
  dev_size: 50
