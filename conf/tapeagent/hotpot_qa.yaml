hydra:
  job:
    chdir: True
  run:
    dir: outputs/${exp_name}_seed${seed}    
target: evaluate
exp_name: exp
seed: 2
agent: agentic_rag
llm_cache: true
load_demos: false
optimize:
  do: false
  n_demos: 2
rag:
  partial_demos: true
  demos: true
agentic_rag:
  max_hops: 2
dataset:
  dev_size: 50
