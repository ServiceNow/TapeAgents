defaults:
  - gaia
  - llm: gpt4o_mini
  - _self_
  
exp_name: gpt4o_mini_long_steps_1
exp_path: ../gaia/runs/${exp_name}
data_dir: ../gaia/dataset/validation/
n_attempts: 1
agent:
  planning_mode: simple
  subtasks: false

hydra:
  run:
    dir: ../gaia/runs/${exp_name}