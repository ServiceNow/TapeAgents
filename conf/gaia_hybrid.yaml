defaults:
  - llm: gpt4o_mini
  - llm@plan_llm: gpt4o
  - _self_
  
exp_name: hydrid_val
exp_path: outputs/gaia/runs2/${exp_name}
split: validation
batch: 1

agent:
  plain_code: false

env:
  attachment_dir: ${exp_path}/attachments/
  image_observations: true
  use_web_cache: true

tasks:
  1: [40]

studio:
  tape:
    null

hydra:
  run:
    dir: ${exp_path}
