defaults:
  - llm: gpt4o_mini
  - _self_
  
exp_name: gpt4o_mini_val_code1
exp_path: ../gaia/runs/${exp_name}
data_dir: ../gaia/dataset/validation/
n_attempts: 1
batch: 32
agent:
  planning_mode: simple
  subtasks: false

env:
  image_observations: true
  use_web_cache: true

hydra:
  run:
    dir: ../gaia/runs/${exp_name}
