defaults:
  - llm: gpt4o_mini
  - _self_
  
exp_name: gpt4o_mini_val_batch32_6
exp_path: outputs/gaia/runs/${exp_name}
split: validation
n_attempts: 1
batch: 32
agent:
  plain_code: false

env:
  image_observations: true
  use_web_cache: true

hydra:
  run:
    dir: ${exp_path}
