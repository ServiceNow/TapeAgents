defaults:
  - llm: gpt4o_mini
  - _self_
  
exp_name: gpt4o_mini_val_changes_t0
exp_path: ../gaia/runs/${exp_name}
data_dir: ../gaia/dataset/validation/
batch: 32
agent:
  planning_mode: simple

env:
  safe_calculator: false
  use_web_cache: true
  only_cached_webpages: false

hydra:
  run:
    dir: ../gaia/runs/${exp_name}
