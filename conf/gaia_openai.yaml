defaults:
  - llm: gpt4o_mini
  - _self_
  
exp_name: gpt4o_mini_search1
exp_path: outputs/gaia/runs/${exp_name}
split: validation
batch: 32

agent:
  plain_code: false

env:
  attachment_dir: ${exp_path}/attachments/
  image_observations: true
  use_web_cache: true

hydra:
  run:
    dir: ${exp_path}
