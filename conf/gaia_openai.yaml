defaults:
  - llm: gpt4o_mini
  - _self_
  
exp_name: gpt4o_mini_val_gym1

exp_path: outputs/gaia/runs/${exp_name}
split: validation
batch: 32

level: 1
task: 0

agent:
  plain_code: false

env:
  use_web_cache: true

studio:
  tape:
    null

hydra:
  run:
    dir: ${exp_path}
