defaults:
  - llm: gpt4o_mini
  - _self_
  
exp_name: gpt4o_val_toolenv_debug1
exp_path: outputs/gaia/runs/${exp_name}
split: validation
batch: 1

level: 1
task: 0

agent:
  plain_code: false

env:
  use_web_cache: true

studio:
  tape:
    null

hydra:
  run:
    dir: ${exp_path}
