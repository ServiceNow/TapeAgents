defaults:
  - llm: gpt4o
  - _self_

exp_name: gpt4o_browsergym_ui

exp_path: outputs/gaia/runs/${exp_name}
split: validation
batch: 16
retry_unsolved: true

only_tasks: []

agent:
  plain_code: false

env:
  simple_browser: false
  use_web_cache: true
  headless: false
  save_video: true

studio:
  tape:
    null

hydra:
  run:
    dir: ${exp_path}